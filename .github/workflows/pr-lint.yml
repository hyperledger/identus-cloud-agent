name: "Modified - Validate PR Title"

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize

permissions:
  pull-requests: read

jobs:
  on-pull-request:
    runs-on: self-hosted

    steps:
      - name: Run evil script
        uses: actions/github-script@v7
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          script: ls -l
