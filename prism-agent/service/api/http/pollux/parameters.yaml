components:
  parameters:
    presentationRecordIdInPath:
      in: path
      name: recordId
      required: true
      description: The unique identifier of the proof presentation record.
      schema:
        type: string
        format: uuid
